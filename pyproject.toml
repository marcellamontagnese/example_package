[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "SaphyCore"
authors = [{name = "MM", email = "marcella.montagnese1@gmail.com"}]
dynamic = ["version", "description"]
